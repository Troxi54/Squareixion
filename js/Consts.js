BigNumber.set({
    RANGE: Math.floor(76081 ** 1.5),
    });

let elements;

const setting = {
    save: true,
    auto_save: 10,
    update_time_s: BigNumber('1e-0'),
    to_exp: BigNumber('1e6'),
    star_count: 0,
    cube_text_plus_style: "-text",
    cube_name_postfixes: ['-', '', '+', '++' ],
    cube_size_start: 50,
}

const unlocks = {
    prestige: BigNumber('4e0')
}